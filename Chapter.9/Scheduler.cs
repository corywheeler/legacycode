namespace Chapter._9
{
	public class Scheduler
	{
		public Scheduler()
		{
			// To further demonstrate the Onion Parameter, this class should take
			// more objects into it's contructor. And those objects should take other
			// objects.

			// For brevity, I'm breaking the chain here. Don't want to continue
			// going too deep, and would rather focus on the technique to break the
			// dependency.
		}
	}
}